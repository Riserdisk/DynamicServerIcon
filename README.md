# DynamicServerIcon

A server-side Fabric mod for Minecraft 1.21.1 that automatically rotates the multiplayer server icon at configurable intervals.

## Features

- Server-side only
- Automatic icon rotation
- 64×64 PNG validation
- Configurable icon directory
- No client mod required
